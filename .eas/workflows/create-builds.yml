name: Create builds

on:
  push:
    branches: ['main']
  workflow_dispatch: {}

jobs:
  build_ios:
    name: Build iOS app
    type: build
    params:
      platform: ios
      profile: production
  build_android:
    name: Build Android app
    type: build
    params:
      platform: android
      profile: production
